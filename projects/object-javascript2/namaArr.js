let nama = ["Dian", "Andri", "Irawan"];
const cobaArray = (arr) => {
  document.write(arr);
};

cobaArray(nama[2]);

// const loopArray = () => {
//   let nama = new Array("Dian", "Andri", "Irawan");
//   for (let i = 0; i < nama.length; i++) {
//     document.writeln(`<h4>${nama[i]}</h4>`);
//   }
// };

// loopArray();
